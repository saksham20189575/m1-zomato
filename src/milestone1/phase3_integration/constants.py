"""Phase 3 — candidate cap and defaults for LLM context bounds."""

from __future__ import annotations

# Doc: cap for LLM context (e.g. 15–50).
DEFAULT_CANDIDATE_CAP = 30

MIN_CANDIDATE_CAP = 1
MAX_CANDIDATE_CAP = 200
